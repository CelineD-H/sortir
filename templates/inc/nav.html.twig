<nav>
    <ul>
        <a href="{{ path('app_home') }}" title="Page d'accueil"><li>Page d'accueil</li></a>

        {% if app.user %}
            <a href="{{ path('ville_create') }}" title="Ville"><li>Ville</li></a>
            <a href="{{ path('user_view', {'id': app.user.id}) }}" title="Profil"><li>Mon profil</li></a>
            <a href="{{ path('app_logout') }}" title="Déconnexion"><li>Déconnexion</li></a>
        {% else %}
            <a href="{{ path('app_login') }}" title="Connexion"><li>Connexion</li></a>
            <a href="{{ path('app_register') }}" title="Inscription"><li>Inscription</li></a>
        {% endif %}
    </ul>
</nav>