{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} | CSV
{% endblock %}

{% block body %}
    <div class="container">
        <h2>Insertion d'utilisateur par fichier csv</h2>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Soumettre</button>
        {{ form_end(form) }}
    </div>
{% endblock %}